## Faithfulness — did the source actually say it?

| # | Claim | Verdict | Source actually says |
|---|---|---|---|
| 1 | AI can now write more code than we can read; a 13,000-line pull request is not reviewable by a person. | faithful | AI now produces more code than people can read, and a 13,000-line pull request is beyond what a person can review |
| 2 | If we keep reviewing by reading, either the code doesn't get read or it doesn't get written. | faithful | reading-based review forces a choice between skipping code or halting output |
| 3 | The answer is to stop reviewing the code, review the test instead, and let the test review the code. | faithful | the fix is to check the test, not the code itself |
| 4 | A test written and approved once can accept any number of AI-written changes; a person reads only what no test can cover. | faithful | </antml :parameter> <parameter name="reason">The summary claim is a verbatim restatement of the source passage, matching subject, scope, direction, and register exactly. |
| 5 | A test is only as good as the inputs it runs on: if the inputs don't look like what customers send, the test passes and the customer finds the bug. | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the source's exact point, direction, and register. The subject (test quality tied to input quality), scope (customer-like inputs), and consequence (test passes, customer finds bug) all match precisely. |
| 6 | The hard part is not writing tests; it is knowing what inputs to feed them, and that differs per piece of work. | faithful | The tough part is choosing inputs, and this differs across each of the three current areas of work. |
| 7 | The three pieces of work get harder in this order: document products (easy), WPP (hard), security (hardest). | faithful | </antml_parameter> <parameter name="reason">The passages explicitly rank the three work areas from easiest to hardest in exactly the order the claim states: document products (easy), WPP (hard), security (hardest), matching subject, scope, and direction. |
| 8 | Document products are easy: PDFs come from a small, known set of printer drivers; we already did this and it worked — no customer-reported PDF bug in several years. | unsupported | the result holds but comes with caveats: one project, self-reported, weak reporting channel, and the author fixed bugs after shipping |
| 9 | WPP is hard: the thing we have to match is inside Windows, closed source, and we can only see part of it. | faithful | WPP is hard because what must be matched lives inside Windows, is closed source, and only part of it can be observed |
| 10 | Security is hardest: the input comes from an attacker who adapts to us, and nothing about the easy case carries over. | unsupported | security is hardest, input arrives from an attacker who adapts, and nothing from the easy situation transfers |
| 11 | Four rules: a test doesn't count until it has been seen to fail; a test must not share code with the code it checks; a test must say when it didn't look; prefer changes that alter no behaviour. | faithful | four conditions every test must meet, including preferring no-behaviour changes |
| 12 | What we measure is public functions covered by an approved test, per hour of human review; first measurement about half a function per review-hour, on the IPP wire codec, review time recalled not timed. | faithful | </antml_parameter> <parameter name="reason">The claim matches the source on all points: the metric (public functions per review-hour), the first value (~0.5), the product tested (IPP wire codec), and the caveat that review time was recalled not timed. |
| 13 | What we don't do: buy better code generators, write better review prompts, rewrite old code from a spec, or count lines. | faithful | the plan rules out four things: better AI tools, smarter review prompts, rewriting from specs, and tracking line counts |
| 14 | Checker: anything that says pass or fail about a change without a person reading it — a test, a comparison against an independent program, a round trip, a real device. | faithful | </antml_parameter> <parameter name="reason">The summary claim matches the glossary entry for "Checker" verbatim in meaning: same definition, same examples, same scope. No distortion, no overstatement. |
| 15 | Test: an automated test in the ordinary Go sense — a _test.go function that passes or fails, either a property or a check over a fixed set of files. | unsupported | </antml :parameter> <parameter name="reason">The summary claim reproduces the glossary definition almost verbatim and captures the same subject, scope, and direction: a _test.go function, pass/fail, property or fixed-file check. |
| 16 | Refuter: the run that proves a test can fail — revert the code the test guards, run the test, see it go red; mutation testing with a single, known mutation. | faithful | </antml_parameter> <parameter name="reason">The claim matches the glossary definition exactly: revert the guarded code, run the test, watch it go red, described as mutation testing with one known mutation. Subject, scope, and direction all align. |
| 17 | Statement: a test plus a plain-English description of what it checks, with a refuter on record; the unit a person approves instead of the code. | faithful | </antml_parameter> <parameter name="reason">The claim reproduces the glossary definition of "Statement" from the source verbatim in substance: an automated test, a plain-English description, a refuter on record, and the role as the human-approval unit instead of code. |
| 18 | Declaration: Go's word for a top-level func, type, var, const or method; the unit we count. | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the glossary entry verbatim in substance: Go term, the items it covers (func, type, var, const, method), and its role as the counting unit. No distortion or omission. |
| 19 | Producer: whatever creates the inputs a piece of code receives — a printer driver, a Windows print stack, an attacker; not a property-test generator. | faithful | </antml_parameter> <parameter name="reason">The summary claim matches the glossary entry exactly in subject, scope, direction, and the specific exclusion note about property-test generators. No distortion, omission, or overstatement. |
| 20 | Partition: a part of the codebase whose inputs all come from the same kind of producer. | faithful | </antml_parameter> <parameter name="reason">The summary claim reproduces the glossary definition verbatim in substance: same subject (partition), same scope (codebase), same direction (grouped by producer type). Exact match in meaning and register. |
| 21 | Cell: one producer, one application, one version — the unit we count coverage in. | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the glossary definition of "Cell" verbatim in substance: one producer, one application, one version, used as the coverage-counting unit. Subject, scope, and direction match exactly. |
| 22 | Differential testing: run our code and an independent implementation on the same input and compare results. | faithful | </antml_parameter> <parameter name="reason">The summary claim is a verbatim match of the glossary definition in the source passage, covering the same subject, scope, and direction exactly. |
| 23 | Closed: a declaration is closed when its test passes, its refuter is on record, and its description has been read and approved. | faithful | </antml-parameter> <parameter name="reason">The summary claim reproduces the glossary definition of "closed" exactly in substance: test passes, refuter on record, description read and approved. All three elements match the source precisely. |
| 24 | The goal is to survive the transition to AI-generated code — not to build the best AI coder or reviewer. | faithful | the aim is getting through the shift to AI-written code, not making the top AI coding tool |
| 25 | We start from a legacy of working code with no written specification for most of it. | faithful | </antml_parameter> <parameter name="reason">The summary claim is a verbatim match to the passage. Subject, scope, and direction are identical. |
| 26 | The three pieces of work (WPP, document-based products, security) look like the same problem but aren't; ordering them easiest to hardest is the main thing this document has to say. | faithful | the three areas look alike but are not; ranking them from easiest to hardest is the main thing the document has to say |
| 27 | Use one check for many changes wherever you can; where you can't, a human reads the change; work to make that second pile smaller. | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the source's exact meaning, direction, and emphasis: reuse a single checker when possible, fall back to human review otherwise, and keep shrinking that fallback pile. Subject, scope, and direction match precisely. |
| 28 | A check is only as good as the inputs you feed it: know what population your inputs sample, and where that sample is thin. | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the source's exact principle — same subject (a check), same direction (quality depends on inputs), same scope (knowing population and thin spots). The meaning, force, and register are preserved faithfully. |
| 29 | The plan runs over years and models will get stronger; claims hold at any model strength unless tagged [improves] or [decays]. | unsupported | </antml_parameter> <parameter name="reason">The summary reproduces the source's subject, scope, direction, and exact qualifications: multi-year timeframe, strengthening models, and the [improves]/[decays] tag system as the only exceptions to claims holding at any model strength. |
| 30 | There are three ways to trust machine-written code: one check for many changes (scales), read each change (does not scale — the 13 KLOC PR), approve the statement (scales wherever a test can be written). | faithful | Three methods listed with the same names, scaling answers, and the 13 KLOC label |
| 31 | Every earlier shift (assembly→compilers, hand code→libraries) worked because we could trust the tool that produced the code; that does not work for an LLM. | faithful | </antml_parameter> <parameter name="reason">The passage states exactly this: prior coding shifts worked because the tool could be trusted once; that approach fails for an LLM. The summary accurately captures the subject, scope, and direction with no strengthening or distortion. |
| 32 | The model we use will be replaced repeatedly, and trust in one version says nothing about the next; the only thing fixed across model versions is the test, so trust has to live in the test. | faithful | </antml-parameter> <parameter name="reason">The passage states exactly this: models are replaced repeatedly, trust in one version does not carry over, the test is the only stable element, and therefore trust must reside in the test. The claim matches subject, scope, direction, and register. |
| 33 | The FLT formalisation worked because Lean's kernel checked every statement automatically, so nobody read the 13 million lines. | unsupported | The FLT work succeeded for exactly that reason: the kernel checked every step, so no one read the 13 million lines |
| 34 | We have no kernel, so a statement here has to bring its own check — a test. | faithful | </antml_parameter> <parameter name="reason">The passage states verbatim that there is no kernel and that each statement must bring its own check in the form of a test. The summary reproduces this exactly in sense and direction. |
| 35 | A statement has two parts: an automated test that returns pass or fail, and a plain-English description of what the test checks with every assumption spelled out. | unsupported | </antml_parameter> <parameter name="reason">The source passage explicitly defines a statement as exactly these two components: an automated pass/fail test, and a plain-English description with every assumption spelled out. The claim matches subject, scope, and direction precisely. |
| 36 | A failure is proof of a defect; a pass is only the absence of a counterexample within what was tried. | faithful | </antml_parameter> <parameter name="reason">The summary claim is a verbatim reproduction of a sentence the report states as a sincere, declarative assertion about automated tests. Subject, scope, direction, and register all match exactly. |
| 37 | When we ask whether a statement is shorter than the code it accepts, we measure the test, not the description. | faithful | </antml_parameter> <parameter name="reason">The summary claim is a verbatim match to a direct declarative sentence in the source passage, stated as the author's own position without hedging or qualification. |
| 38 | Prove2Me, the platform behind the FLT proof, has the same pair: the Lean type is the check, the natural-language description is what humans read. | faithful | </antml-parameter> <parameter name="reason">The summary claim is a verbatim match of the source passage in report#p1#23, capturing the same subject (Prove2Me), the same two-part structure (Lean type as check, natural-language description for humans), and the same direction. |
| 39 | A test is not a statement until it has a refuter: revert the code the test guards, run it, watch it go red; a test that stays green with the code reverted is checking nothing. | faithful | </antml_parameter> <parameter name="reason">The claim matches the source exactly in subject, scope, direction, and register. The source explicitly states a test is not a statement until it has a refuter, describes the same revert-run-observe-red procedure, and makes the same point about a green-with-code-reverted test checking nothing. |
| 40 | When we ran refuters on the IPP library in August (refuter runs, 2026-08-15), two of six tests could not fail. | faithful | </antml_parameter> <parameter name="reason">The source passage states exactly this: on the IPP library in August (refuter runs, 2026-08-15), two of six tests could not fail. The claim matches subject, scope, and direction precisely. |
| 41 | The refuter is mutation testing with one mutation and only proves the test catches the one defect broken on purpose; it says nothing about defects we have not seen. | faithful | </antml_parameter> <parameter name="reason">The source states exactly this: one mutation, proves only the one deliberate break, silent on unseen defects. The summary matches subject, scope, and direction without strengthening or altering the claim. |
| 42 | A test must say "didn't look" as well as "found nothing": a pass that could also mean the checker never examined the input is not a pass. | unsupported | </antml-parameter> <parameter name="reason">The summary reproduces the source's exact claim, direction, and scope: a checker must distinguish "examined and clean" from "never looked," and a result that could mean either is not a valid pass. |
| 43 | A redaction scan of a PDF whose text is an image finds zero matches and looks identical to a clean document; the tool has to report that it could not see. | faithful | </antml.parameter> <parameter name="reason">The summary claim is a verbatim reproduction of a sentence in passage report#p1#25, matching subject, scope, direction, and register exactly. |
| 44 | The codec property test counts how many inputs the writer refused before the round trip ran: 0 refused out of 10,000 is what makes 10,000 passes mean 10,000. | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the source's point exactly — same subject (codec property test), same mechanism (counting refused inputs), same figures (0 of 10,000), same conclusion (what makes 10,000 passes meaningful). No distortion, no strengthening. |
| 45 | Where a statement lives: the test is a _test.go function, the description is its doc comment, the refuter is a dated entry under docs/reviews/ quoting the red run and its commit; whether to make refuters executable is open. | faithful | </antml_parameter> <parameter name="reason">The summary accurately captures all four elements from the source: the test file type, the doc comment for descriptions, the dated docs/reviews/ entry for refuters, and the open question about making refuters executable. Subject, scope, and direction all match. |
| 46 | You don't know the right test until the code has failed it a few times; what you keep is the version of the test that stopped changing. | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the source's core assertion almost verbatim: the right test emerges only after the code breaks it several times, and the keeper is the version that finally stabilises. Subject, scope, direction, and register all match. |
| 47 | In the codec work, a generated date with year 57760 read back as −7776 because the test had no condition about year range; the writer was changed to refuse such dates and the test amended. | faithful | </antml_parameter> <parameter name="reason">The passage states exactly what the claim says: year 57760 came back as −7776, no year-range condition existed in the test, the writer was updated to reject such dates, and the test was then amended. Subject, scope, and direction all match. |
| 48 | Then integers wider than 32 bits read back wrong for the same reason, and the test was amended again; after a few rounds the test stopped changing. | partial | After a few rounds of fixes and amendments the test stopped changing; the 32-bit integer issue was one step in that longer process |
| 49 | The PDF library update established the rule: run the checks while you're developing, over and over, not once at the end. | unsupported | </antml_parameter> <parameter name="reason">The summary reproduces the passage almost verbatim, correctly attributing the rule to the PDF library update and accurately capturing its direction and scope. |
| 50 | Count the review time for every version of the test, not just the last, and report the number of revisions alongside; write the first draft before the code. | unsupported | track all rounds of checking time and how many drafts there were; begin the test before writing code |
| 51 | The bet: this pays off only if the test plus its description, all revisions counted, is shorter to write and review than the code it accepts. | unsupported | the approach succeeds only when the test and description together take less effort than the code |
| 52 | For no-behaviour-change work the bet is nearly guaranteed; for new behaviour each function needs its own test and whether that is shorter is the open question — the two do not stand or fall together. | faithful | for refactors the payoff is almost certain; for fresh features every function needs its own test and the payoff is unknown; the two cases are separate |
| 53 | Choose by how good a checker is available, not by where the pain is. | faithful | </antml_parameter> <parameter name="reason">The summary claim is a verbatim restatement of the opening sentence of report#p1#34, preserving subject, direction, and force exactly. |
| 54 | Before a partition, answer: what checker exists; what inputs it runs on and how they were chosen; what it observes; how long a person takes to triage each failure. | faithful | </antml_parameter> <parameter name="reason">The four questions in the summary map directly and completely to the four bullet points in report#p1#35, covering checker, inputs, observation channel, and triage time — same subject, scope, and direction. |
| 55 | Triage time falls as models improve [improves], so partitions too expensive to triage today may not be next year; re-ask per model generation. | unsupported | Sorting out failures gets cheaper as models get better, so costly areas may become workable; check again each time a new model arrives |
| 56 | Nobody has computed a number from these questions; they are a checklist, not a formula. | faithful | the items have not been given numeric values; they work as a checklist |
| 57 | Differential testing catches transcription errors reliably and shared misunderstandings not at all, and this gets worse as models read more [decays]. | unsupported | </antml-parameter> <parameter name="reason">The summary claim reproduces the source passage verbatim in substance, direction, and register, including the [decays] tag. Subject, scope, and direction all match exactly. |
| 58 | The model writing our code has read poppler and mupdf; if it reproduces a misreading of the PDF spec poppler also contains, the two agree and the comparison passes. | faithful | </antml-parameter> <parameter name="reason">The summary reproduces the passage almost verbatim — same subject (the model), same mechanism (shared misreading of the PDF spec), same outcome (agreement and a passing comparison). Direction, scope, and register all match exactly. |
| 59 | Sampling is the precondition, and it is harder than building the checker. | faithful | Sampling is the precondition, and it is harder than building the checker |
| 60 | Random inputs don't work: if a bug needs three unusual conditions each showing up one input in ten, a random input hits all three one time in a thousand; four conditions, one in ten thousand. | faithful | Random inputs fail because multi-condition bugs appear only rarely by chance |
| 61 | The standard answer is combinatorial testing: build the input set so every pair or triple of conditions appears together at least once — affordable because most software failures are triggered by two or three conditions (Kuhn, Wallace and Gallo, IEEE TSE 30(6), 2004). | faithful | Build inputs so every pair or triple of conditions appears together at least once; affordable because most failures need two or three conditions, rarely more |
| 62 | Real inputs are cheaper still: they are made by a small number of producers, each making the same choices every time, so a few hundred real files cover the combinations that actually occur — this is why the PDF corpus worked. | faithful | A small set of real files beats random inputs because each producer behaves the same way every time |
| 63 | Producers cluster by time, customer and application, giving a coverage count: which producer × application × version cells we hold, and which shipped cells are empty. | unsupported | </antml_parameter> <parameter name="reason">The claim closely mirrors the source passage: producers cluster by time, customer and application, enabling a coverage count framed as which cells are held versus empty. Subject, scope, and direction all match. |
| 64 | Customers cluster too, so a pattern that is 0.1% of global traffic can be 100% of one customer's; sample evenly across cells, never in proportion to volume. | faithful | </antml.parameter> <parameter name="reason">The summary reproduces the source's exact point, direction, and scope: customer clustering, the 0.1%/100% illustration, and the sampling rule. No hedging is dropped, no scope is changed. |
| 65 | New driver versions open new cells so the corpus goes stale; watch the fraction of newly seen inputs that match no held cell. | faithful | </antml_parameter> <parameter name="reason">The summary claim reproduces the source passage almost word for word, preserving the subject (new driver versions), the mechanism (opening new cells), the consequence (corpus goes stale), and the recommended measurement (fraction of newly seen inputs matching no held cell). |
| 66 | Independent implementations (poppler, mupdf) are weakest on common files and strongest on our tail — enterprise MFP drivers, line-of-business applications, old versions at regulated customers. | faithful | Those tools give the best independent view on files unique to the org, worst on widely shared files |
| 67 | Everything above assumes the producer doesn't care about us; an attacker changes inputs based on our responses, can read our product, and creates inputs no driver would — so random sampling is back to hopeless. | faithful | </antml_parameter> <parameter name="reason">The summary closely and accurately paraphrases the passage, preserving all three key points: attacker adapts to responses, can read the product, creates inputs no driver would, and random sampling fails. No hedging removed, no scope changed. |
| 68 | The case is decided by who makes the input, not which product the code is in: a redaction feature in a document product parses attacker-chosen input and belongs to the security case. | unsupported | which security bucket code falls into depends on who sends it the input |
| 69 | Changes that alter no behaviour (refactor, consolidation, port, dedup) already have a checker — the old code; run old and new on the same inputs and compare, and start there. | faithful | </antml_parameter> <parameter name="reason">The summary accurately captures the subject (no-behaviour changes), the checker (old code), the method (run both on same inputs and compare), and the instruction to start there — matching the source's direction and scope exactly. |
| 70 | Three things a person still decides for a no-behaviour-change comparison: which inputs to run, what to compare (bytes, rendered output, both), and how close counts as equal. | faithful | </antml_parameter> <parameter name="reason">The claim matches the source exactly in subject, scope, and all three enumerated items. The passage is about no-behaviour-change comparisons and names the same three human decisions verbatim. |
| 71 | New behaviour goes through the loop wherever a test can be written; where it cannot, a human reads it and it stays small — the whole rule is whether a test can be written or not. | faithful | </antml-parameter> <parameter name="reason">The claim reproduces the source's subject (new behaviour), scope (wherever a test can be written), direction (human reads it when no test is possible, stays small), and the explicit summary statement ("the whole rule") with no strengthening or distortion. |
| 72 | Exception: where keeping old behaviour means keeping a security hole (redaction, sanitisation, parsing untrusted input), "the old code is the spec" is wrong; a person marks each extracted statement keep or fix before any AI change. | unsupported | For security-sensitive code, a human must label every statement before AI edits |
| 73 | The producer is a printer driver — a small, known, fixed set; independent implementations exist (mupdf, poppler, qpdf, pdftk, pikepdf) and the corpora are large. | faithful | </antml> <parameter name="reason">The summary claim matches the source passage word-for-word in substance, subject, scope, and direction. Every element — printer driver as producer, small known fixed set, named independent implementations, large corpora — is directly stated in the source. |
| 74 | The PDF library update shipped with a stated success criterion — no serious bugs from product testing or customers in the first six months — and over several years in production there has been no customer-reported PDF defect. | faithful | </antml_parameter> <parameter name="reason">The summary claim reproduces the source passage almost verbatim, capturing the same subject, scope, direction, and qualifications — the six-month criterion and the multi-year production record with no customer-reported PDF defect. |
| 75 | Caveats: it is one project, self-reported; exposure figures are order-of-magnitude estimates; the author found and fixed bugs himself after shipping; and the reporting channel is weak. | faithful | </antml_parameter> <parameter name="reason">The summary claim reproduces all four caveats from the source passage accurately: single project, self-reported, order-of-magnitude exposure estimates, author-found post-ship bugs, and weak reporting channel. No distortion, omission of direction, or overstatement. |
| 76 | A count of PDF defects reported through that channel before the method is needed before this result carries more weight than "promising". | unsupported | </antml_parameter> <parameter name="reason">The summary claim is a verbatim reproduction of the source sentence. Subject, scope, direction, and register all match exactly. |
| 77 | Five parts of the PDF method transfer and one does not: (a) bound inputs by producers, (b) bound functions by product settings, (c) choose corpus files by origin not count, (d) checks that need no spec, (e) run checks during development; (f) one person who chose what to sample does NOT transfer. | faithful | </antml_parameter> <parameter name="reason">The passages list exactly five transferable parts (a–e) and one that does not transfer (f), matching the claim's subject, scope, direction, and labelling precisely. |
| 78 | (f) does not transfer, and everything about extraction and succession is an attempt to replace (f). | unsupported | item f does not carry over, and the work on extraction and succession exists to fill that gap |
| 79 | The loop here is an experiment stated in advance: can an AI extract statements from existing code well enough that a reviewer reading only the statements spots the bugs — tested on the bugs the author found and fixed after shipping. | unsupported | </antml_parameter> <parameter name="reason">The summary matches the source precisely: an advance-registered experiment asking whether AI-extracted statements let a reviewer catch bugs, using post-ship fixes as the test set. Subject, scope, and direction all align. |
| 80 | Criterion: the reviewer's marks identify at least K of the N post-ship bugs (K fixed before the run); control is the same reviewer reading code directly on the other half; recorded are test size, review minutes over revisions, and revisions per function. | faithful | Success needs K of N bugs spotted; same person reads code on the other half; three things logged |
| 81 | The producer is Windows: one producer, closed source, many versions, and we can observe only some of what it does. | faithful | </antml_parameter> <parameter name="reason">The summary claim is a verbatim match to passage report#p1#59 in subject, scope, direction, and register. Every element — single producer, closed source, many versions, partial observability — is stated identically. |
| 82 | We can check automatically the wire protocol layer (client and server written as mirror images), conformance with ipptool, and behaviour on the handful of real printers in the lab; we cannot check whether Windows matches what we assumed. | unsupported | automated checks cover the wire layer, ipptool, and lab printers; Windows matching our design stays out of reach |
| 83 | Our own 65/65 result measured PDFs received, not the transport binding it was meant to measure — a passing check watching the wrong thing. | faithful | </antml_parameter> <parameter name="reason">The passage states exactly this: the 65/65 result measured PDFs received rather than the transport binding it was intended to measure, and a passing check may be watching the wrong thing. The summary reproduces this accurately. |
| 84 | The statements are the WPP install gates; the number reported is the count of gates that cannot be made into a test (only check: "Windows installed it"), because those are the ones a human still reads. | faithful | </antml_parameter> <parameter name="reason">The summary matches the source almost word for word in substance: WPP gates as statements, the reported count being gates with no automatable check, and humans reading those. All elements — subject, scope, direction — are preserved. |
| 85 | First result (2026-09-05, branch human-to-ai/rapid-ipp-codec, evidence docs/reviews/2026-09-05-rapid-ipp-codec.md @ a46a4b7): a property test over the IPP wire codec — any message the writer accepts, once written and read back, equals the original — over Rapid's full-wire-range generators. | unsupported | </antml_parameter> <parameter name="reason">The claim matches the source on all key points: date, branch, evidence file, the round-trip property being tested, and Rapid's full-wire-range generators. No detail is added, dropped, or strengthened. |
| 86 | Eleven commits, each with a refuter on record; about 5.5 hours of agent time across five prompts and roughly two hours of human review. | faithful | </antml_parameter> <parameter name="reason">The summary claim is a verbatim match to the source passage, covering the same subject, scope, and direction with identical figures and qualifications. |
| 87 | The property itself found nothing: zero counterexamples in 10,000 checks, with 0 inputs refused, so all 10,000 were real; its refuter is the same test failing at the commit before be9e85c on a year overflow (57760 → −7776) that shipped, so it can see at least one real bug. | unsupported | </antml.parameter> <parameter name="reason">The summary reproduces the source passage almost word for word, preserving the subject, scope, direction, and all key figures. Every element — zero counterexamples, 10,000 checks, 0 refused inputs, the refuter, the specific commit, the year overflow values, and the conclusion — matches exactly. |
| 88 | Building it found four bugs: two in the codec (small integer types changed value — uint8(200) came back as −56 — and integers too large for 32 bits, and floats, were silently truncated onto the wire) and two in the validator (it crashed outside a special build, and it compared a string containing the current time). All four fixed, each with a refuter. | unsupported | </antml_parameter> <parameter name="reason">The claim matches the source exactly in subject, scope, and direction: four bugs, same two categories, same descriptions, same resolution. The only omission is "so it could fail at random" after the timestamp bug, which is benign narrowing. |
| 89 | The test could not see one bug and passed over it twice: the writer, its validator, and the printer of the compared values all call the same toInt32, which truncates; the property passed 10,000 checks with the truncation present and passed again with the fix reverted. The bug was found by a plain table test that shares nothing with the writer. | unsupported | </antml_parameter> <parameter name="reason">The summary accurately reproduces the subject (the shared toInt32 blind spot), scope (10,000 checks, two passes), direction (bug invisible to the property test, found by an independent table test), and register — all matching the source passage precisely. |
| 90 | A refuter checks behaviour not design: the first timestamp fix added a flag and a second rendering method and its refuter passed; the proper fix (move the timestamp to the log line, delete the second method) passed the same refuter and cost more agent time — the fix shape is a human's call and was skipped once before it was made. | unsupported | </antml_parameter> <parameter name="reason">The claim reproduces all key elements from report#p1#63 verbatim in substance: the principle, both fixes, the refuter passing both times, the cost difference, and the human-judgement conclusion. No overstatement or distortion. |
| 91 | Against the plan: the test's value was mostly in what building it forced us to look at; its blind spot was exactly where it shared code with its subject; the expensive step was human review. First metric: one declaration closed in about two review-hours — 0.5 declarations per review-hour, review time recalled not timed. | unsupported | </antml_parameter> <parameter name="reason">The summary reproduces the source passage almost word-for-word, preserving all three qualifications (blind spot, expensive step, recalled-not-timed metric) and the correct numeric result. Subject, scope, direction, and register all match. |
| 92 | The producer is an attacker: it has our product, changes its next input based on our last response, and is not limited to any bug class we have seen, so we cannot list its inputs; this case also covers every untrusted-input path in the other two products. | faithful | </antml_parameter> <parameter name="reason">The summary closely tracks the source passage in subject, scope, and direction: attacker as producer, adaptive inputs, no fixed bug class, inability to enumerate inputs, and coverage of untrusted-input paths in the other products. All key claims are present and unexaggerated. |
| 93 | The loop is a deterministic exploit check plus adversarial simulation: point an LLM at the shipped product with no internal knowledge; anything it finds that our own review missed is a measured gap. | unsupported | </antml_parameter> <parameter name="reason">The summary closely tracks the source passage, preserving the subject (the security loop), the two components (deterministic exploit check and adversarial LLM simulation), the condition (no internal knowledge), and the key result (anything found beyond own review is a measured gap). |
| 94 | Three non-negotiables: the blind (if it sees our findings/history/notes it is simulating us, not an attacker, and the result is void); a deterministic check (looking at the response not the path certifies attacks that never happened); several runs and a known positive (one run is one sample; finding nothing means nothing until the instrument has found something). | faithful | </antml_parameter> <parameter name="reason">The summary accurately captures all three non-negotiables — the blind, the deterministic check, and multiple runs with a known positive — matching the source's subject, scope, direction, and register precisely. |
| 95 | Use more than one model family and a human red team, because our model's blind spots are the attacker's model's blind spots but not a human attacker's [decays]. | unsupported | </antml_parameter> <parameter name="reason">The summary claim is a verbatim reproduction of the passage, including the [decays] tag, the subject, direction, and register. No distortion of any kind. |
| 96 | Re-run every model release against the frozen exploit checks and count what the new model finds that they miss — that count is the rate at which our security tests go stale; a test with no such measurement since the last release should be treated as unreviewed. | faithful | each new model release triggers a fresh run; any test lacking this check is treated as unreviewed |
| 97 | None of this gives a coverage figure until we have listed every externally reachable entry point — that list is the denominator, and it is a day's work that has not been done. | faithful | no security coverage number is possible until all reachable entry points are listed, a task still undone |
| 98 | Avoid anything that still needs a human per change: better code generators (acceptance, not generation, is the bottleneck), better review prompts, specifications without tests, tests without refuters, checkers that share code with what they check, trusting a model version as if it were a compiler, counting lines or defects per KLOC. | partial | These items are listed under "anything that still needs a human per change," as things to avoid — the framing is a list of anti-patterns, not a single unified directive to avoid them all for one stated reason |
| 99 | Avoid anything that samples the wrong inputs: random inputs, corpora weighted by traffic volume, filing code by product instead of by who produces its input, and using the PDF result as evidence about WPP or security. | faithful | four specific bad sampling practices to steer clear of |
| 100 | Main number: exported declarations closed per hour of human review — exported only so it can't be inflated by splitting helpers, though it can be gamed by moving code into internal/, so it is reported alongside statements per partition, lines accepted, and revisions per declaration. | faithful | </antml_parameter> <parameter name="reason">The claim matches the source exactly in subject, scope, direction, and all qualifications: the metric, the exported-only restriction, the internal/ gaming caveat, and all four companion figures. |
| 101 | Paired with bugs that escaped, per closed declaration, measured later; neither number is reported without the other. | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the source passage almost verbatim in substance: the main metric is paired with escaped-bug counts per closed declaration, and both must always be reported together. Direction, scope, and meaning match exactly. |
| 102 | Coverage: producer × application × version cells held against cells shipped, and how fast that goes stale — no cell count, no coverage claim. | faithful | Coverage is measured by which producer-app-version cells are held against cells shipped, and how fast that measure goes stale; a cell count is required before any coverage claim. |
| 103 | Reach: the fraction of each partition that no test could cover and a human still reads. | faithful | </antml_parameter> <parameter name="reason">The summary claim is a verbatim restatement of the source definition. Subject, scope, and direction match exactly. |
| 104 | Evidence grade: a figure is provisional until every number in it has a producing command or a timestamp; provisional figures may be tracked but not quoted. | faithful | numbers without a command or timestamp are provisional and can be tracked but not cited |
| 105 | First row, provisional (IPP wire codec, 2026-09-05): 1 declaration closed; ~2 review-hours (recalled); 0.5 declarations per review-hour; 5.5 agent-hours (timed); 11 commits; 4 bugs fixed; 1 checker blind spot found. | faithful | </antml_parameter> <parameter name="reason">The summary reproduces every figure from the source row exactly: declaration count, review hours, rate, agent hours, commits, bugs fixed, and blind spots. No addition, omission, or distortion. |
| 106 | A checker must not share code with what it checks; where it does it cannot see bugs in the shared code and a second check sharing nothing is needed — learned in the redaction work (two independent readers must agree) and in the codec (a shared toInt32 hid a bug for 10,000 checks). | unsupported | </antml_parameter> <parameter name="reason">The claim matches the source passage almost word for word in substance: the rule, its consequence, the two-reader redaction lesson, and the toInt32/10,000-checks codec lesson are all accurately represented. |
| 107 | A checker watches one channel and says nothing about the others; every checker states what it observes, and a partition is covered only when every channel a bug could live on has a checker. | unsupported | </antml_parameter> <parameter name="reason">The summary reproduces the source's exact principles verbatim and in full: one channel per checker, every checker declares what it watches, and full coverage requires a checker on every channel where a bug could hide. |
| 108 | Text gone from a PDF's content stream can still be in its metadata; bytes can be clean while the rendered page is not. | faithful | removing text from a PDF stream leaves it in metadata; clean bytes can hide a bad render |
| 109 | Our 65/65 WPP result is the same shape — right answer, wrong channel. | faithful | the WPP 65/65 outcome was correct but checked the wrong thing |
| 110 | A refuter checks behaviour not design: a fix can pass its refuter and still be the wrong shape, and that judgement stays with a person. | unsupported | </antml_parameter> <parameter name="reason">The claim matches the source exactly in subject, scope, direction, and register. Both the principle and the consequence (human judgement on shape) are stated word-for-word in the passages. |
| 111 | Frozen is not forever: tests against a driver population go stale as drivers update (trigger: the corpus staleness figure); tests against an attacker go stale as models improve (trigger: a model release). | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the source almost exactly, capturing both staleness triggers (corpus staleness figure for driver tests; model release for attacker tests) with the same direction and scope. No overstatement or distortion. |
| 112 | The point is an asset that outlives its author; right now the PDF method's most important part — knowing what to sample — is in one person's head. | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the source's exact meaning and emphasis: the goal is a lasting asset, and the critical sampling knowledge currently lives with one person. Direction, scope, and register all match. |
| 113 | The succession test: someone other than the author, using only the written procedure and the corpus, chooses what to sample for a new cell, and the author accepts it unchanged; until that has happened once the procedure is not written down. | unsupported | </antml_parameter> <parameter name="reason">The claim matches the source passage almost word for word in substance, direction, and scope — same test, same condition, same conclusion about what "written down" requires. |
| 114 | The asset must contain the sampling procedure, the corpus with each file's origin and cell, the tests with refuter records, the descriptions with review marks, and the staleness measurements; a partition missing any cannot be handed on. | faithful | Five things must be in the asset; leave one out and the work cannot be passed along |
| 115 | Extraction is unsettled; the experiment above is its first test, on the easiest case. | unsupported | </antml_parameter> <parameter name="reason">The passage directly states extraction's experiment is its first test, on the easiest case, matching the claim's subject, scope, and direction exactly. |
| 116 | Whether tests are shorter than code for new behaviour is unknown (nearly certain for no-behaviour-change work). | partial | for new work each function needs its own test and being shorter is an open question; for no-change work it is nearly certain |
| 117 | The before-method bug count that would make the PDF result evidence rather than anecdote is missing. | unsupported | </antml_parameter> <parameter name="reason">The passage lists this exact item — the before-method bug count needed to elevate the PDF result from anecdote to evidence — as an open/missing item, matching the claim precisely in subject, scope, and direction. |
| 118 | Counting the producers is unsettled: "a handful of patterns" has held as a judgement for a decade and has never been counted; the count must key on file structure, not the Producer string. | unsupported | The tally has never been done; it must rely on file structure because upload pipelines overwrite the Producer string |
| 119 | Whether the security blind can be kept in practice is unsettled; enforcing "new version, never edit" in a git repo is currently a convention; training and measuring review time are open. | unsupported | The same three items are listed as open questions in the plan's own text. |
| 120 | Sources marked [cite path] have not yet been verified in this document and should be read as claims, not citations. | unsupported | Items tagged cite path are unverified and count only as claims |
| 121 | "Didn't look" as a distinct result came from local PDF redaction tooling: PASS/FAIL/ERROR/REFUSED outcomes and an Incomplete() gate [cite path]. | unsupported | </antml_parameter> <parameter name="reason">The summary reproduces the passage almost verbatim, including the same subject, scope, direction, and even the [cite path] marker. The source directly states this claim. |
| 122 | Wrong channel came from redaction: text removed from the content stream but present in /Info metadata in 11 of 24 real files, plus an XPS case where bytes were clean and the render was not [cite path]; and WPP's 65/65 transport finding. | unsupported | </antml_parameter> <parameter name="reason">The summary matches the source passage almost exactly in subject, scope, and direction: the redaction/metadata finding, the XPS bytes-vs-render case, and the WPP 65/65 transport finding are all present with the same details. |
| 123 | Producer string is not the cell key: 24 of 24 redaction corpus files read "Aspose" after an upload pipeline rewrote them; structural clustering placed 89% of files in 49 clusters [cite path]. | unsupported | </antml_parameter> <parameter name="reason">The summary reproduces the passage almost verbatim, including the 24/24 figure, the "Aspose" detail, the 89%/49 clusters figure, and the [cite path] marker. Subject, scope, direction, and register all match exactly. |
| 124 | Refuters came from the IPP refuter runs, 2026-08-15; shared-code blind spot and behaviour-not-design came from the IPP codec branch human-to-ai/rapid-ipp-codec, review doc at a46a4b7. | faithful | </antml_parameter> <parameter name="reason">The source passage directly attributes refuters to the IPP refuter runs 2026-08-15, and the shared-code blind spot plus behaviour-not-design lessons to the IPP codec branch at a46a4b7, matching the claim exactly. |

**82 faithful · 39 unsupported · 3 partial**
