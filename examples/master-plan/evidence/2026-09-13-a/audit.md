## Faithfulness — did the source actually say it?

| # | Claim | Verdict | Source actually says |
|---|---|---|---|
| 1 | AI can now write more code than we can read; a 13,000-line pull request is not reviewable by a person. | faithful | AI output exceeds what humans can read; a 13k-line PR is too big for any person to check |
| 2 | If we keep reviewing by reading, either the code doesn't get read or it doesn't get written. | faithful | </antml-parameter> <parameter name="reason">The summary claim is a verbatim match to the source passage. The subject, scope, direction, and register are identical. No distortion, hedging removed, or context stripped. |
| 3 | The answer is to stop reviewing the code, review the test instead, and let the test review the code. | faithful | stop reading code; look at the test instead, and have the test do the checking of code |
| 4 | A test written and approved once can accept any number of AI-written changes; a person reads only what no test can cover. | faithful | </antml-parameter> <parameter name="reason">The summary reproduces the source's exact proposition, direction, and scope: a single approved test handles unlimited AI changes, and human reading is limited to what tests cannot reach. |
| 5 | A test is only as good as the inputs it runs on: if the inputs don't look like what customers send, the test passes and the customer finds the bug. | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the source's exact point, direction, and scope: test quality depends on input quality, and mismatched inputs mean customers discover failures instead of tests. Fully faithful. |
| 6 | The hard part is not writing tests; it is knowing what inputs to feed them, and that differs per piece of work. | faithful | </antml_parameter> <parameter name="reason">The passage directly states both propositions in the claim: the hard part is knowing what inputs to feed tests, and this varies per piece of work. The claim faithfully captures subject, scope, and direction. |
| 7 | The three pieces of work get harder in this order: document products (easy), WPP (hard), security (hardest). | faithful | </antml_parameter> <parameter name="reason">The source explicitly ranks the three areas in exactly this order and uses the same labels: document products (easy), WPP (hard), security (hardest). The claim faithfully captures subject, scope, and direction. |
| 8 | Document products are easy: PDFs come from a small, known set of printer drivers; we already did this and it worked — no customer-reported PDF bug in several years. | unsupported | The same claim appears but with notable caveats: one project, self-reported, weak reporting channel, and the author fixed bugs himself after shipping |
| 9 | WPP is hard: the thing we have to match is inside Windows, closed source, and we can only see part of it. | faithful | WPP is difficult because the target lives inside Windows, is closed source, and only parts are visible |
| 10 | Security is hardest: the input comes from an attacker who adapts to us, and nothing about the easy case carries over. | unsupported | The source says the same thing in the same words and with the same force |
| 11 | Four rules: a test doesn't count until it has been seen to fail; a test must not share code with the code it checks; a test must say when it didn't look; prefer changes that alter no behaviour. | faithful | four rules govern how tests earn trust in this method |
| 12 | What we measure is public functions covered by an approved test, per hour of human review; first measurement about half a function per review-hour, on the IPP wire codec, review time recalled not timed. | faithful | </antml_parameter> <parameter name="reason">The claim matches the source exactly: same metric (public functions per review-hour), same first result (about half), same product (IPP wire codec), same caveat (review time recalled not timed). |
| 13 | What we don't do: buy better code generators, write better review prompts, rewrite old code from a spec, or count lines. | faithful | The plan rules out four things: better AI tools, smarter review prompts, rewriting from specs, and tracking line counts. |
| 14 | Checker: anything that says pass or fail about a change without a person reading it — a test, a comparison against an independent program, a round trip, a real device. | faithful | </antml_parameter> <parameter name="reason">The summary claim reproduces the glossary definition of "Checker" almost verbatim, capturing the same subject, scope, direction, and register as the source passage. |
| 15 | Test: an automated test in the ordinary Go sense — a _test.go function that passes or fails, either a property or a check over a fixed set of files. | unsupported | </antml :parameter> <parameter name="reason">The claim reproduces the glossary definition verbatim in substance: a _test.go function that passes or fails, either a property or a fixed-file check. Subject, scope, and direction all match exactly. |
| 16 | Refuter: the run that proves a test can fail — revert the code the test guards, run the test, see it go red; mutation testing with a single, known mutation. | faithful | </antml_parameter> <parameter name="reason">The claim matches the glossary definition and elaboration in the passages exactly: revert the guarded code, run the test, watch it fail; one deliberate mutation, checked once. |
| 17 | Statement: a test plus a plain-English description of what it checks, with a refuter on record; the unit a person approves instead of the code. | faithful | </antml_parameter> <parameter name="reason">The summary claim reproduces the glossary definition of "Statement" verbatim in substance: an automated test, a plain-English description, a refuter on record, and the unit a person approves instead of reading code. All elements match exactly. |
| 18 | Declaration: Go's word for a top-level func, type, var, const or method; the unit we count. | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the glossary entry verbatim in substance: same term, same definition, same scope. No distortion, no qualification dropped. |
| 19 | Producer: whatever creates the inputs a piece of code receives — a printer driver, a Windows print stack, an attacker; not a property-test generator. | faithful | </antml_parameter> <parameter name="reason">The summary matches the glossary entry exactly in subject, scope, direction, and register — the same definition of "Producer," the same examples, and the same distinction from a property-test generator. |
| 20 | Partition: a part of the codebase whose inputs all come from the same kind of producer. | faithful | </antml_parameter> <parameter name="reason">The claim reproduces the glossary definition verbatim in substance. Subject, scope, and direction match exactly. |
| 21 | Cell: one producer, one application, one version — the unit we count coverage in. | faithful | </antml_parameter> <parameter name="reason">The summary claim reproduces the glossary definition verbatim in substance: same subject (cell), same three-part definition (one producer, one application, one version), same role (coverage unit). Fully faithful. |
| 22 | Differential testing: run our code and an independent implementation on the same input and compare results. | faithful | run our code and a separate tool on the same input, then check if results match |
| 23 | Closed: a declaration is closed when its test passes, its refuter is on record, and its description has been read and approved. | faithful | </antml_parameter> <parameter name="reason">The summary claim is a verbatim restatement of the glossary definition in the passage. Subject, scope, direction, and all three conditions match exactly. |
| 24 | The goal is to survive the transition to AI-generated code — not to build the best AI coder or reviewer. | faithful | the aim is getting through the shift to AI-written code, not making the top AI coding tool |
| 25 | We start from a legacy of working code with no written specification for most of it. | faithful | </antml.parameter> <parameter name="reason">The summary claim is a verbatim reproduction of a sentence from the source passage. Subject, scope, and direction all match exactly. |
| 26 | The three pieces of work (WPP, document-based products, security) look like the same problem but aren't; ordering them easiest to hardest is the main thing this document has to say. | faithful | the three cases look alike but differ; arranging them from simplest to toughest is the document's main point |
| 27 | Use one check for many changes wherever you can; where you can't, a human reads the change; work to make that second pile smaller. | faithful | apply a single checker to many changes; when that fails, a person reviews it; shrink that person-reviewed pile |
| 28 | A check is only as good as the inputs you feed it: know what population your inputs sample, and where that sample is thin. | faithful | </antml_parameter> <parameter name="reason">The summary claim reproduces the source's exact meaning, direction, and scope — the same principle about checker quality depending on input population and thin coverage — stated with the same force and register as in the passage. |
| 29 | The plan runs over years and models will get stronger; claims hold at any model strength unless tagged [improves] or [decays]. | unsupported | </antml_parameter> <parameter name="reason">The summary restates the passage almost exactly in substance: multi-year timeline, models improving, and the same tagging convention for exceptions. Subject, scope, and direction all match. |
| 30 | There are three ways to trust machine-written code: one check for many changes (scales), read each change (does not scale — the 13 KLOC PR), approve the statement (scales wherever a test can be written). | faithful | Three ways listed, with the same scaling verdicts for each |
| 31 | Every earlier shift (assembly→compilers, hand code→libraries) worked because we could trust the tool that produced the code; that does not work for an LLM. | faithful | </antml_parameter> <parameter name="reason">The passage states exactly this: prior coding shifts worked by trusting the tool once, and that approach fails for an LLM. The claim matches the subject, scope, direction, and force of the source. |
| 32 | The model we use will be replaced repeatedly, and trust in one version says nothing about the next; the only thing fixed across model versions is the test, so trust has to live in the test. | faithful | </antml_parameter> <parameter name="reason">The passage states exactly what the claim says: models will be replaced repeatedly, trust in one version does not carry over, the test is the only stable element, and therefore trust must reside in the test. |
| 33 | The FLT formalisation worked because Lean's kernel checked every statement automatically, so nobody read the 13 million lines. | unsupported | The FLT work succeeded for that exact reason: the kernel checked each step on its own, so no one read the 13 million lines |
| 34 | We have no kernel, so a statement here has to bring its own check — a test. | faithful | without a kernel, every statement must carry its own automated check |
| 35 | A statement has two parts: an automated test that returns pass or fail, and a plain-English description of what the test checks with every assumption spelled out. | unsupported | </antml_parameter> <parameter name="reason">The source explicitly defines a statement as exactly these two parts: an automated pass/fail test, and a plain-English description with every assumption spelled out. The claim matches subject, scope, and direction precisely. |
| 36 | A failure is proof of a defect; a pass is only the absence of a counterexample within what was tried. | faithful | </antml_parameter> <parameter name="reason">The summary claim is a verbatim reproduction of a sincere, declarative assertion made in the report passage. Subject, scope, direction, and register all match exactly. |
| 37 | When we ask whether a statement is shorter than the code it accepts, we measure the test, not the description. | faithful | </antml_parameter> <parameter name="reason">The summary claim is a verbatim match of a direct, unhedged assertion in the source passage, stated as the author's own position with no qualification or change of meaning. |
| 38 | Prove2Me, the platform behind the FLT proof, has the same pair: the Lean type is the check, the natural-language description is what humans read. | faithful | </antml_parameter> <parameter name="reason">The summary claim is a verbatim match to passage report#p1#23, preserving subject, scope, direction, and register exactly. |
| 39 | A test is not a statement until it has a refuter: revert the code the test guards, run it, watch it go red; a test that stays green with the code reverted is checking nothing. | faithful | </antml_parameter> <parameter name="reason">The claim matches the source exactly in subject, direction, and force: a test needs a refuter (revert, run, see red); one that stays green after reversion proves nothing. No hedging is dropped or added. |
| 40 | When we ran refuters on the IPP library in August (refuter runs, 2026-08-15), two of six tests could not fail. | faithful | </antml_parameter> <parameter name="reason">The passage states exactly what the claim says: on the IPP library in August (refuter runs, 2026-08-15), two of six tests could not fail. Subject, scope, and direction match precisely. |
| 41 | The refuter is mutation testing with one mutation and only proves the test catches the one defect broken on purpose; it says nothing about defects we have not seen. | faithful | </antml_parameter> <parameter name="reason">The source states exactly the same two-part limitation: one mutation, proves only the chosen defect is caught, silent on unseen defects. Subject, scope, and direction match precisely. |
| 42 | A test must say "didn't look" as well as "found nothing": a pass that could also mean the checker never examined the input is not a pass. | unsupported | </antml_parameter> <parameter name="reason">The summary reproduces the source's subject, scope, direction, and register with no strengthening, narrowing, or distortion. The wording is essentially identical to the passage. |
| 43 | A redaction scan of a PDF whose text is an image finds zero matches and looks identical to a clean document; the tool has to report that it could not see. | faithful | </antml_parameter> <parameter name="reason">The summary claim reproduces the passage verbatim in substance, direction, and register. The subject, scope, and meaning are identical. |
| 44 | The codec property test counts how many inputs the writer refused before the round trip ran: 0 refused out of 10,000 is what makes 10,000 passes mean 10,000. | faithful | </antml.parameter> <parameter name="reason">The summary reproduces the source's point exactly: the codec test tracks refusals before the round trip, and zero refusals out of 10,000 is what gives the 10,000 passes their meaning. Wording, subject, scope, and direction all match. |
| 45 | Where a statement lives: the test is a _test.go function, the description is its doc comment, the refuter is a dated entry under docs/reviews/ quoting the red run and its commit; whether to make refuters executable is open. | faithful | </antml_parameter> <parameter name="reason">The claim matches the source exactly: test location, description location, refuter location (dated docs/reviews/ entry with red-run quote and commit), and the open question about executable refuters — all present with the same meaning and register. |
| 46 | You don't know the right test until the code has failed it a few times; what you keep is the version of the test that stopped changing. | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the source's meaning, direction, and phrasing almost exactly. Both the "fails a few times" point and "the version that stopped changing" point match the passage verbatim in substance. |
| 47 | In the codec work, a generated date with year 57760 read back as −7776 because the test had no condition about year range; the writer was changed to refuse such dates and the test amended. | faithful | A date value came back wrong; the test lacked a year-range check, so the writer was blocked from those dates and the test was updated. |
| 48 | Then integers wider than 32 bits read back wrong for the same reason, and the test was amended again; after a few rounds the test stopped changing. | partial | After the 32-bit issue the test was changed again, and the final stable version became the accepted statement — but this was one step in a longer chain starting with a year-range bug. |
| 49 | The PDF library update established the rule: run the checks while you're developing, over and over, not once at the end. | unsupported | </antml_parameter> <parameter name="reason">The summary reproduces the source passage almost verbatim, capturing the same subject (PDF library update), the same rule (run checks repeatedly during development), and the same direction. No distortion, overstatement, or omission. |
| 50 | Count the review time for every version of the test, not just the last, and report the number of revisions alongside; write the first draft before the code. | unsupported | Track time across all test versions, show the revision count, and make drafting the test before writing code a habit |
| 51 | The bet: this pays off only if the test plus its description, all revisions counted, is shorter to write and review than the code it accepts. | unsupported | the approach pays off only if the test plus its description, counting every revision, is shorter to write and review than the code it accepts |
| 52 | For no-behaviour-change work the bet is nearly guaranteed; for new behaviour each function needs its own test and whether that is shorter is the open question — the two do not stand or fall together. | faithful | no-change work is almost certain to pay off; fresh features each need a separate check and savings there are unknown; the two cases are independent |
| 53 | Choose by how good a checker is available, not by where the pain is. | faithful | Pick work based on checker quality, not on what hurts most |
| 54 | Before a partition, answer: what checker exists; what inputs it runs on and how they were chosen; what it observes; how long a person takes to triage each failure. | faithful | Four questions to ask before starting a partition, covering the same four topics |
| 55 | Triage time falls as models improve [improves], so partitions too expensive to triage today may not be next year; re-ask per model generation. | unsupported | </antml_parameter> <parameter name="reason">The source passage states exactly this: triage cost drops as models improve, so currently too-expensive partitions may become viable later, and the question should be revisited each model generation. The summary faithfully captures subject, direction, and the [improves] tag. |
| 56 | Nobody has computed a number from these questions; they are a checklist, not a formula. | faithful | </antml_parameter> <parameter name="reason">The summary claim matches the source almost word-for-word in meaning: no numbers have been computed, and the items function as a checklist rather than a formula. Subject, scope, and direction are identical. |
| 57 | Differential testing catches transcription errors reliably and shared misunderstandings not at all, and this gets worse as models read more [decays]. | unsupported | </antml_parameter> <parameter name="reason">The summary claim is a verbatim reproduction of the source passage, including the subject, direction, and the [decays] tag. Subject, scope, and direction all match exactly. |
| 58 | The model writing our code has read poppler and mupdf; if it reproduces a misreading of the PDF spec poppler also contains, the two agree and the comparison passes. | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the source's exact reasoning: the AI model trained on poppler/mupdf may replicate a shared spec misreading, causing both to agree and the differential test to pass. Subject, scope, direction, and register all match. |
| 59 | Sampling is the precondition, and it is harder than building the checker. | faithful | Knowing what to feed the checker is the precondition, and it is harder than building the checker |
| 60 | Random inputs don't work: if a bug needs three unusual conditions each showing up one input in ten, a random input hits all three one time in a thousand; four conditions, one in ten thousand. | faithful | Random testing fails for multi-condition bugs because the odds multiply sharply |
| 61 | The standard answer is combinatorial testing: build the input set so every pair or triple of conditions appears together at least once — affordable because most software failures are triggered by two or three conditions (Kuhn, Wallace and Gallo, IEEE TSE 30(6), 2004). | faithful | </antml_parameter> <parameter name="reason">The claim matches the source exactly in subject, scope, direction, and citation. The summary accurately captures the technique, its affordability rationale, and the supporting reference. |
| 62 | Real inputs are cheaper still: they are made by a small number of producers, each making the same choices every time, so a few hundred real files cover the combinations that actually occur — this is why the PDF corpus worked. | faithful | Producers always make the same choices, so a few hundred real files cover the combinations that occur — that is why the PDF corpus worked |
| 63 | Producers cluster by time, customer and application, giving a coverage count: which producer × application × version cells we hold, and which shipped cells are empty. | unsupported | </antml_parameter> <parameter name="reason">The claim matches the source almost verbatim in subject, scope, and direction: producers cluster by time/customer/application, enabling a coverage count framed as which cells are held versus empty. |
| 64 | Customers cluster too, so a pattern that is 0.1% of global traffic can be 100% of one customer's; sample evenly across cells, never in proportion to volume. | faithful | </antml_parameter> <parameter name="reason">The claim matches the source exactly in subject, scope, direction, and wording: customers cluster, a tiny share of global traffic can be all of one customer's, and the rule is to sample evenly across cells rather than by volume. |
| 65 | New driver versions open new cells so the corpus goes stale; watch the fraction of newly seen inputs that match no held cell. | faithful | </antml_parameter> <parameter name="reason">The summary claim reproduces the source sentence verbatim in substance: same subject (driver versions / cells / corpus staleness), same direction, same monitoring recommendation. Fully faithful. |
| 66 | Independent implementations (poppler, mupdf) are weakest on common files and strongest on our tail — enterprise MFP drivers, line-of-business applications, old versions at regulated customers. | faithful | those tools were built on public files everyone uses, so they give fresh insight on the rare producers only we see |
| 67 | Everything above assumes the producer doesn't care about us; an attacker changes inputs based on our responses, can read our product, and creates inputs no driver would — so random sampling is back to hopeless. | faithful | </antml_parameter> <parameter name="reason">The summary closely mirrors the source passage in subject, scope, and direction: the prior assumption about passive producers, the attacker's three distinguishing traits, and the conclusion that random sampling fails. No material distortion. |
| 68 | The case is decided by who makes the input, not which product the code is in: a redaction feature in a document product parses attacker-chosen input and belongs to the security case. | unsupported | what matters is who supplies the input, and a redaction tool belongs in the security bucket |
| 69 | Changes that alter no behaviour (refactor, consolidation, port, dedup) already have a checker — the old code; run old and new on the same inputs and compare, and start there. | faithful | </antml_parameter> <parameter name="reason">The summary accurately captures the subject (no-behaviour-change edits), the checker (old code, run both on same inputs), the instruction to start there, and even the specific examples (refactor, consolidation, port, dedup). Direction and scope match exactly. |
| 70 | Three things a person still decides for a no-behaviour-change comparison: which inputs to run, what to compare (bytes, rendered output, both), and how close counts as equal. | faithful | humans decide inputs, what to measure, and the closeness standard |
| 71 | New behaviour goes through the loop wherever a test can be written; where it cannot, a human reads it and it stays small — the whole rule is whether a test can be written or not. | faithful | </antml_parameter> <parameter name="reason">The summary closely mirrors the source passage, preserving subject (new behaviour), scope (wherever a test can be written), direction (human reads what cannot be tested, kept small), and the explicit framing that this is "the whole rule." No hedging is dropped or strengthened. |
| 72 | Exception: where keeping old behaviour means keeping a security hole (redaction, sanitisation, parsing untrusted input), "the old code is the spec" is wrong; a person marks each extracted statement keep or fix before any AI change. | unsupported | security-sensitive code needs human review of every statement before AI edits it |
| 73 | The producer is a printer driver — a small, known, fixed set; independent implementations exist (mupdf, poppler, qpdf, pdftk, pikepdf) and the corpora are large. | faithful | </antml_parameter> <parameter name="reason">The summary claim reproduces the source passage almost verbatim, capturing the same subject (printer driver as producer), scope (document products), and all specific details (named implementations, corpus size). |
| 74 | The PDF library update shipped with a stated success criterion — no serious bugs from product testing or customers in the first six months — and over several years in production there has been no customer-reported PDF defect. | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the source passage almost verbatim, capturing the same subject, scope, direction, and register — the stated success criterion and the multi-year absence of customer-reported PDF defects. |
| 75 | Caveats: it is one project, self-reported; exposure figures are order-of-magnitude estimates; the author found and fixed bugs himself after shipping; and the reporting channel is weak. | faithful | </antml_parameter> <parameter name="reason">The summary claim closely matches the four caveats stated in the source: single project, self-reported, order-of-magnitude exposure estimates, post-ship author fixes, and weak reporting channel. All elements are present and accurately represented. |
| 76 | A count of PDF defects reported through that channel before the method is needed before this result carries more weight than "promising". | unsupported | prior PDF defect data is needed before the result means more than promising |
| 77 | Five parts of the PDF method transfer and one does not: (a) bound inputs by producers, (b) bound functions by product settings, (c) choose corpus files by origin not count, (d) checks that need no spec, (e) run checks during development; (f) one person who chose what to sample does NOT transfer. | faithful | </antml_parameter> <parameter name="reason">The source lists all six items (a)–(f) with exactly the same meaning: (a)–(e) carry over, (f) — the one expert who decided what to sample — does not. The claim matches subject, scope, and direction precisely. |
| 78 | (f) does not transfer, and everything about extraction and succession is an attempt to replace (f). | unsupported | part f cannot be carried over, and all work on extraction and succession aims to fill that gap |
| 79 | The loop here is an experiment stated in advance: can an AI extract statements from existing code well enough that a reviewer reading only the statements spots the bugs — tested on the bugs the author found and fixed after shipping. | unsupported | </antml.parameter> <parameter name="reason">The passage matches the claim exactly in subject, scope, and direction: a pre-registered experiment asking whether AI-extracted statements let a reviewer catch post-ship bugs the author later fixed. All key elements are present without strengthening or distortion. |
| 80 | Criterion: the reviewer's marks identify at least K of the N post-ship bugs (K fixed before the run); control is the same reviewer reading code directly on the other half; recorded are test size, review minutes over revisions, and revisions per function. | faithful | The success bar, comparison group, and logged data for the experiment |
| 81 | The producer is Windows: one producer, closed source, many versions, and we can observe only some of what it does. | faithful | </antml_parameter> <parameter name="reason">The summary claim is a verbatim match of passage report#p1#59, capturing the same subject, scope, and direction with identical meaning. |
| 82 | We can check automatically the wire protocol layer (client and server written as mirror images), conformance with ipptool, and behaviour on the handful of real printers in the lab; we cannot check whether Windows matches what we assumed. | unsupported | Those same three things are auto-checkable; Windows matching our assumptions is not |
| 83 | Our own 65/65 result measured PDFs received, not the transport binding it was meant to measure — a passing check watching the wrong thing. | faithful | the 65/65 check looked at PDFs coming in, not the network layer it was supposed to cover |
| 84 | The statements are the WPP install gates; the number reported is the count of gates that cannot be made into a test (only check: "Windows installed it"), because those are the ones a human still reads. | faithful | WPP gates that only Windows can verify are counted, because a person must still read those |
| 85 | First result (2026-09-05, branch human-to-ai/rapid-ipp-codec, evidence docs/reviews/2026-09-05-rapid-ipp-codec.md @ a46a4b7): a property test over the IPP wire codec — any message the writer accepts, once written and read back, equals the original — over Rapid's full-wire-range generators. | unsupported | </antml_parameter> <parameter name="reason">The claim matches the source on all key points: date, branch, evidence file, the round-trip property being tested, and Rapid's full-wire-range generators. No detail is added, dropped, or strengthened. |
| 86 | Eleven commits, each with a refuter on record; about 5.5 hours of agent time across five prompts and roughly two hours of human review. | faithful | </antml_parameter> <parameter name="reason">The summary claim is a verbatim match to the passage, covering the same subject, scope, and direction with identical figures and qualifications. |
| 87 | The property itself found nothing: zero counterexamples in 10,000 checks, with 0 inputs refused, so all 10,000 were real; its refuter is the same test failing at the commit before be9e85c on a year overflow (57760 → −7776) that shipped, so it can see at least one real bug. | unsupported | </antml_parameter> <parameter name="reason">The summary reproduces the source passage almost verbatim, preserving the subject, scope, direction, and all key details — zero counterexamples, 10,000 checks, 0 refused inputs, the refuter failing on the year-overflow bug. Fully faithful. |
| 88 | Building it found four bugs: two in the codec (small integer types changed value — uint8(200) came back as −56 — and integers too large for 32 bits, and floats, were silently truncated onto the wire) and two in the validator (it crashed outside a special build, and it compared a string containing the current time). All four fixed, each with a refuter. | unsupported | </antml_parameter> <parameter name="reason">The claim matches the source exactly in subject, scope, direction, and detail: four bugs, two in the codec, two in the validator, with the same descriptions and the same conclusion that all four were fixed with refuters. |
| 89 | The test could not see one bug and passed over it twice: the writer, its validator, and the printer of the compared values all call the same toInt32, which truncates; the property passed 10,000 checks with the truncation present and passed again with the fix reverted. The bug was found by a plain table test that shares nothing with the writer. | unsupported | </antml :parameter> <parameter name="reason">The summary reproduces the source's subject, scope, direction, and register with high precision — same bug, same mechanism, same outcome, same resolution. No material detail is added, dropped, or strengthened. |
| 90 | A refuter checks behaviour not design: the first timestamp fix added a flag and a second rendering method and its refuter passed; the proper fix (move the timestamp to the log line, delete the second method) passed the same refuter and cost more agent time — the fix shape is a human's call and was skipped once before it was made. | unsupported | </antml_parameter> <parameter name="reason">The summary accurately captures all key elements: the principle, the two fixes, both passing the same refuter, the cost difference, and that the correct shape is a human judgment that was deferred once. No distortion or overstatement. |
| 91 | Against the plan: the test's value was mostly in what building it forced us to look at; its blind spot was exactly where it shared code with its subject; the expensive step was human review. First metric: one declaration closed in about two review-hours — 0.5 declarations per review-hour, review time recalled not timed. | unsupported | </antml_parameter> <parameter name="reason">The summary reproduces the source passage almost verbatim, capturing the same subject, direction, qualifications, and metric. Every element — blind spot, shared code, human review cost, the 0.5 figure, and the recalled-not-timed caveat — matches exactly. |
| 92 | The producer is an attacker: it has our product, changes its next input based on our last response, and is not limited to any bug class we have seen, so we cannot list its inputs; this case also covers every untrusted-input path in the other two products. | faithful | </antml_parameter> <parameter name="reason">The claim closely mirrors the source passage: attacker as producer, adaptive inputs, no bug-class limit, inputs cannot be listed, and coverage of untrusted-input paths in the other two products. All subject, scope, and direction match. |
| 93 | The loop is a deterministic exploit check plus adversarial simulation: point an LLM at the shipped product with no internal knowledge; anything it finds that our own review missed is a measured gap. | unsupported | </antml_parameter> <parameter name="reason">The summary reproduces the source's exact meaning, direction, and register. The source explicitly defines the loop as a deterministic exploit check combined with adversarial LLM simulation, with the same framing of "measured gap" for findings missed by internal review. |
| 94 | Three non-negotiables: the blind (if it sees our findings/history/notes it is simulating us, not an attacker, and the result is void); a deterministic check (looking at the response not the path certifies attacks that never happened); several runs and a known positive (one run is one sample; finding nothing means nothing until the instrument has found something). | faithful | </antml_parameter> <parameter name="reason">The summary accurately captures all three non-negotiables — the blind, the deterministic check, and the multiple-runs requirement — with the same subject, scope, direction, and force as the source passage. |
| 95 | Use more than one model family and a human red team, because our model's blind spots are the attacker's model's blind spots but not a human attacker's [decays]. | unsupported | </antml> <parameter name="reason">The summary reproduces the passage verbatim, including the [decays] tag, the subject, direction, and register. It is a direct and complete match. |
| 96 | Re-run every model release against the frozen exploit checks and count what the new model finds that they miss — that count is the rate at which our security tests go stale; a test with no such measurement since the last release should be treated as unreviewed. | faithful | each new model release triggers a re-run; the gap it finds shows how fast security tests age; tests without that check count as unreviewed |
| 97 | None of this gives a coverage figure until we have listed every externally reachable entry point — that list is the denominator, and it is a day's work that has not been done. | faithful | no security coverage number is possible until all entry points are listed, a task still undone |
| 98 | Avoid anything that still needs a human per change: better code generators (acceptance, not generation, is the bottleneck), better review prompts, specifications without tests, tests without refuters, checkers that share code with what they check, trusting a model version as if it were a compiler, counting lines or defects per KLOC. | partial | The source lists those items under things to avoid, but only the first two (better generators, better review prompts) are explicitly tied to "still a human per change"; the rest are listed as things to avoid for their own stated reasons |
| 99 | Avoid anything that samples the wrong inputs: random inputs, corpora weighted by traffic volume, filing code by product instead of by who produces its input, and using the PDF result as evidence about WPP or security. | faithful | four specific bad practices to steer clear of when picking test inputs |
| 100 | Main number: exported declarations closed per hour of human review — exported only so it can't be inflated by splitting helpers, though it can be gamed by moving code into internal/, so it is reported alongside statements per partition, lines accepted, and revisions per declaration. | faithful | </antml_parameter> <parameter name="reason">The claim matches the source on every point: the metric definition, the exported-only scope, the helper-splitting caveat, the internal/ gaming risk, and all four companion figures. No distortion or omission. |
| 101 | Paired with bugs that escaped, per closed declaration, measured later; neither number is reported without the other. | faithful | </antml-parameter> <parameter name="reason">The summary reproduces the source's exact meaning: the main metric is always paired with escaped-bug data per closed declaration, and both must be reported together. Subject, scope, and direction match precisely. |
| 102 | Coverage: producer × application × version cells held against cells shipped, and how fast that goes stale — no cell count, no coverage claim. | faithful | Coverage is measured by producer times app times version cells held against cells shipped, plus how fast that goes stale; no cell count means no coverage claim |
| 103 | Reach: the fraction of each partition that no test could cover and a human still reads. | faithful | </antml_parameter> <parameter name="reason">The summary claim is a verbatim restatement of the passage's definition of "Reach." Subject, scope, and direction match exactly. |
| 104 | Evidence grade: a figure is provisional until every number in it has a producing command or a timestamp; provisional figures may be tracked but not quoted. | faithful | numbers lacking a command or timestamp are provisional and can be tracked but not cited |
| 105 | First row, provisional (IPP wire codec, 2026-09-05): 1 declaration closed; ~2 review-hours (recalled); 0.5 declarations per review-hour; 5.5 agent-hours (timed); 11 commits; 4 bugs fixed; 1 checker blind spot found. | faithful | the first data row shows one item done, roughly two hours of human time, half an item per hour, and four bugs caught |
| 106 | A checker must not share code with what it checks; where it does it cannot see bugs in the shared code and a second check sharing nothing is needed — learned in the redaction work (two independent readers must agree) and in the codec (a shared toInt32 hid a bug for 10,000 checks). | unsupported | </antml_parameter> <parameter name="reason">The claim matches the source passage almost exactly in subject, scope, direction, and both attributed examples. No overstatement, no fabrication, no distortion. |
| 107 | A checker watches one channel and says nothing about the others; every checker states what it observes, and a partition is covered only when every channel a bug could live on has a checker. | unsupported | </antml_parameter> <parameter name="reason">The summary reproduces the source passage almost verbatim, capturing the same subject (checkers and channels), the same direction (each checker covers one channel; full coverage requires a checker on every channel), and the same force. Faithful. |
| 108 | Text gone from a PDF's content stream can still be in its metadata; bytes can be clean while the rendered page is not. | faithful | </antml_parameter> <parameter name="reason">The summary claim is copied verbatim from the source passage, preserving subject, scope, direction, and register exactly. |
| 109 | Our 65/65 WPP result is the same shape — right answer, wrong channel. | faithful | the WPP 65/65 outcome got the right answer but looked at the wrong thing |
| 110 | A refuter checks behaviour not design: a fix can pass its refuter and still be the wrong shape, and that judgement stays with a person. | unsupported | </antml_parameter> <parameter name="reason">The claim matches the source exactly in subject, direction, and force. Both the principle and its implication — that human judgement is still required — are stated plainly and without hedging in the passages. |
| 111 | Frozen is not forever: tests against a driver population go stale as drivers update (trigger: the corpus staleness figure); tests against an attacker go stale as models improve (trigger: a model release). | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the source's exact meaning, structure, and two triggers with full fidelity — same subject, same direction, same conditions. |
| 112 | The point is an asset that outlives its author; right now the PDF method's most important part — knowing what to sample — is in one person's head. | faithful | </antml_parameter> <parameter name="reason">The summary reproduces the source's exact meaning, direction, and subject: the goal is a lasting asset, and the key sampling knowledge currently sits with one individual. No hedging is removed, no scope is altered. |
| 113 | The succession test: someone other than the author, using only the written procedure and the corpus, chooses what to sample for a new cell, and the author accepts it unchanged; until that has happened once the procedure is not written down. | unsupported | </antml_parameter> <parameter name="reason">The summary reproduces the source's subject, scope, direction, and register with high fidelity — including the specific criterion, the "accepts unchanged" condition, and the "not written down" conclusion. |
| 114 | The asset must contain the sampling procedure, the corpus with each file's origin and cell, the tests with refuter records, the descriptions with review marks, and the staleness measurements; a partition missing any cannot be handed on. | faithful | five things must be in the asset, and any section lacking even one cannot be passed to someone else |
| 115 | Extraction is unsettled; the experiment above is its first test, on the easiest case. | unsupported | </antml;parameter> <parameter name="reason">The passage states exactly this: extraction's first test is the experiment above, on the easiest case. The summary faithfully reproduces the subject, scope, and direction without strengthening or distorting it. |
| 116 | Whether tests are shorter than code for new behaviour is unknown (nearly certain for no-behaviour-change work). | partial | for no-behaviour-change work the test being shorter is nearly guaranteed; for new behaviour it is an open question |
| 117 | The before-method bug count that would make the PDF result evidence rather than anecdote is missing. | unsupported | </antml_parameter> <parameter name="reason">The passage explicitly lists "The before-method bug count that would make the PDF result evidence rather than anecdote" as something still missing. The claim reproduces this accurately. |
| 118 | Counting the producers is unsettled: "a handful of patterns" has held as a judgement for a decade and has never been counted; the count must key on file structure, not the Producer string. | unsupported | the producer count has never been done; file structure is the right basis, not the Producer string |
| 119 | Whether the security blind can be kept in practice is unsettled; enforcing "new version, never edit" in a git repo is currently a convention; training and measuring review time are open. | unsupported | All four items are listed as open questions in the same passage |
| 120 | Sources marked [cite path] have not yet been verified in this document and should be read as claims, not citations. | unsupported | </antml_parameter> <parameter name="reason">The summary reproduces the passage almost verbatim and accurately captures the meaning: tagged sources are unverified and should be treated as claims rather than confirmed citations. |
| 121 | "Didn't look" as a distinct result came from local PDF redaction tooling: PASS/FAIL/ERROR/REFUSED outcomes and an Incomplete() gate [cite path]. | unsupported | </antml_parameter> <parameter name="reason">The summary reproduces the passage almost verbatim, including the same subject, scope, direction, and even the [cite path] marker. The source directly states this claim. |
| 122 | Wrong channel came from redaction: text removed from the content stream but present in /Info metadata in 11 of 24 real files, plus an XPS case where bytes were clean and the render was not [cite path]; and WPP's 65/65 transport finding. | unsupported | </antml_parameter> <parameter name="reason">The summary matches the source passage almost exactly: the redaction/metadata finding (11 of 24 files), the XPS bytes-vs-render case, and the WPP 65/65 transport finding are all present with the same scope and framing, including the [cite path] marker. |
| 123 | Producer string is not the cell key: 24 of 24 redaction corpus files read "Aspose" after an upload pipeline rewrote them; structural clustering placed 89% of files in 49 clusters [cite path]. | unsupported | </antml_parameter> <parameter name="reason">The summary reproduces the passage almost verbatim, including the same figures (24/24, 89%, 49 clusters), the same finding about the upload pipeline rewriting to "Aspose", and the [cite path] marker. Subject, scope, direction, and register all match. |
| 124 | Refuters came from the IPP refuter runs, 2026-08-15; shared-code blind spot and behaviour-not-design came from the IPP codec branch human-to-ai/rapid-ipp-codec, review doc at a46a4b7. | faithful | </antml_parameter> <parameter name="reason">The passage explicitly names both sources in the exact terms the summary uses: refuters traced to the 2026-08-15 IPP runs, and the shared-code blind spot plus behaviour-not-design traced to the rapid-ipp-codec branch at a46a4b7. |

**82 faithful · 39 unsupported · 3 partial**
