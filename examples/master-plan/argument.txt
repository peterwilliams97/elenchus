# argument.txt — "Surviving human-to-AI coding: master plan" (draft v8, 2026-09-06) as an argument tree
# (spec/ARGUMENT.md).
#
# title: Surviving human-to-AI coding — master plan (draft v8, 2026-09-06)
#
# Refinement: root → recommendation → finding → atomic claim (ids in claims-machine.txt).
# This file carries CONTENT and STRUCTURE only. Judgements are DERIVED bottom-up from a merged chain
# (spec/TREE.md), never authored — there is none here.
#
# Line format (indentation = depth; a child sits one level below its parent):
#   <id>[ ?]  | <content, the plan's words, short>  | <edge / provenance note>
# A bare id at claim depth (deepest level) is an ATOMIC-CLAIM LEAF; its text, route and source live in
# claims-machine.txt and are not repeated here.
#
# Edge markers (spec/ARGUMENT.md § Edges):
#   (default)  load-bearing: the recommendation directly rests on the finding, or the finding
#              decomposes to the claim.
#   ?          the plan does NOT tie this node to a single parent — the recommendation is asserted as
#              a rule, its support spread across the document, or (base) it is descriptive material no
#              one recommendation rests on. A question for a human, not a guess.
#
# ATTACHMENT — read this, the plan is NOT a survey. Its "recommendations" are the rules and
# prescriptions it states directly (§The principles, the Four rules, §Choosing, §Sampling, §Eligible,
# §What to avoid, §What we measure, §Succession), so most edges are load-bearing by assertion rather
# than by the plan citing a finding. spec/ARGUMENT.md's partition ("every leaf lands in exactly one
# place") is kept: each claim is attached to the ONE recommendation it most directly serves, and a
# restated figure (e.g. OV8/DP2, WP8/ME6/OV12) is left as a leaf in both the recommendation it serves
# and its restating section only where the pair is an internal-consistency check — otherwise once.
#
# The three cases (document products / WPP / security) are NOT three recommendations; they are one
# recommendation — order the work easiest→hardest by who makes the input (REC9) — whose findings are
# the three cases. The plan's own claim is the ordering, not the cases severally.
#
# Glosses (spec/ARGUMENT.md § define-before-reference): [improves] / [decays] = a claim the plan tags
# as getting easier / harder as models get stronger over the plan's multi-year run. KLOC = thousand
# lines of code. refuter = the run that reverts a fix and watches the guarding test go red (GL3).
# statement = a test PLUS a reviewable plain-English description (GL4). declaration = a Go top-level
# func/type/var/const/method, the unit counted (GL5). cell = one producer × one application × one
# version (GL8). WPP = Windows Protected Print Mode, the closed Windows print stack being matched.
#
# ROOT is the plan's OWN words (§Overview + §Why the test, not the code): stop reviewing the code,
# review the test, because the model that writes the code is replaced repeatedly and the only fixed
# thing across versions is the test — so trust has to live there; the goal is to survive the
# transition to AI-generated code, not to build the best AI coder.

root  | Stop reviewing the code; review the test instead, and let the test review the code — because the model that writes the code will be replaced repeatedly and the only thing fixed across model versions is the test, so trust has to live in the test. The goal is to survive the transition to AI-generated code.  | verbatim = §Overview ("The answer") + §Why the test, not the code

    REC1  | Approve the statement — a test plus a reviewable description — not the code: use one check for many changes, and shrink the pile a human still reads.  | §The principles (first principle, PR1); the plan's core mechanism
        F1  | The only thing fixed across model versions is the test, so trust must live there — every earlier shift trusted the tool, but an LLM will be replaced.  | scheme=practical; §Why the test, not the code
            PR1
            OV3
            OV4
            WT1
            WT2
            WT3
            WT5
            WT4
        F2  | A statement is a test that returns pass/fail plus a plain-English description a person approves.  | scheme=practical; §What a statement is
            ST1
            ST2
            ST3
            ST4
            GL4

    REC2  | A test is not a statement until a refuter has shown it can fail.  | Four rules, rule 1 (OV11) / §What a statement is (ST5)
        F3  | Refuters on the IPP library caught tests that check nothing — two of six could not fail.  | scheme=practical; §What a statement is; the one finding with a HELD truth-maker (refuter-runs-2026-08-15.md)
            OV11
            ST5
            R-REF
            ST6
            GL3
            AP4

    REC3  | A checker must not share code with what it checks.  | Four rules, rule 2 (OV11) / §Tests are code too (TC1)
        F4  | Shared code hides bugs by construction — a shared toInt32 passed 10,000 checks with the bug present and again with the fix reverted.  | scheme=example; §WPP, §Tests are code too, §Choosing (CH6 states the blind spot once)
            TC1
            WP6
            CH6

    REC4  | A checker must say "didn't look", not only "found nothing", and state the one channel it watches.  | Four rules, rule 3 (OV11) / §What a statement is (ST7) / §Tests are code too (TC2)
        F5  | A pass that could mean "never examined the input" is not a pass, and a right answer on the wrong channel is not coverage (the 65/65 WPP result).  | scheme=practical; §What a statement is, §Tests are code too
            ST7
            ST8
            ST9
            TC2
            TC3
            TC5
            WP-65

    REC5  | Prefer changes that alter no behaviour — the old code is the checker — and start there; put new behaviour through the loop only where a test can be written.  | Four rules, rule 4 (OV11) / §Which changes are eligible (EL1)
        F6  | No-behaviour-change work is machine-checkable and the compression bet is nearly guaranteed; whether a test is shorter than the code for NEW behaviour is the open question — the two do not stand or fall together.  | scheme=practical; §Which changes are eligible, §The loop
            EL1
            EL2
            EL3
            EL4
            LP6
            LP7

    REC6  | Iterate the test, not the code, until it stops changing; draft it before the code and count the review time of every revision.  | §The loop
        F7  | You learn the missing condition only after the code fails the test — the codec test grew a year-range and a 32-bit condition round by round until it stabilised.  | scheme=practical; §The loop
            LP1
            LP2
            LP3
            LP4
            LP5

    REC7  | Choose what to work on by how good a checker is available, not by where the pain is.  | §Choosing what to work on
        F8  | The choice is a checklist, not a formula — what checker exists, what inputs, what channel, how long to triage — and no number has been computed from it.  | scheme=example; §Choosing what to work on
            CH1
            CH2
            CH3
            CH4
            CH5

    REC8  | Sample by producer and cell, evenly across cells, never in proportion to traffic volume — random inputs don't work.  | §The principles (second principle, PR2) / §Sampling
        F9  | Real inputs from a few clustered producers cover the multi-condition combinations that reach production, which random and volume-weighted sampling miss.  | scheme=practical; §Sampling; combinatorial basis Kuhn/Wallace/Gallo 2004 (SA3)
            PR2
            SA1
            SA2
            SA3
            SA4
            SA5
            SA6
            SA7
            SA8

    REC9  | Order the work easiest→hardest by who makes the input: document products, then WPP, then security.  | §Overview (OV7) / §Goal (GO3); the three findings are the three cases
        F10  | Document products — easy, already done once: the PDF library shipped and drew no customer-reported defect over several years, with stated caveats.  | scheme=example; §Document-based products; "no bug in N years" claim (DP2/OV8)
            OV8
            DP1
            DP2
            DP3
            DP4
            DP5
            DP6
            DP7
            DP8
        F11  | WPP — hard, closed reference: the first codec property test found nothing itself but forced four bugs into view and measured the metric at 0.5 declarations per review-hour.  | scheme=example; §WPP
            WP1
            WP2
            WP2b
            WP3
            WP3b
            WP4
            WP5
            WP7
            WP8
        F12  | Security — hardest: the attacker adapts and reads our product, so inputs can't be enumerated; no coverage figure exists until every entry point is listed (a day's work not done).  | scheme=example; §Security; §Sampling (SA9/SA10 — the case is decided by who makes the input)
            SA9
            SA10
            SE1
            SE2
            SE3
            SE4
            SE5
            SE6

    REC10  | Measure exported declarations closed per review-hour, paired with escaped bugs and coverage cells; grade a figure provisional until every number in it is reproducible.  | §What we measure
        F13  | First provisional row, IPP wire codec, 2026-09-05: 1 declaration closed, ~2 review-hours, 0.5 declarations per review-hour.  | scheme=practical; §What we measure; restates §WPP (WP8) and §Overview (OV12)
            OV12
            ME1
            ME2
            ME3
            ME4
            ME5
            ME6

    REC11  | Build an asset that outlives its author — a written sampling procedure someone else can use unchanged (succession).  | §Succession
        F14  | The scarce knowledge (what to sample) is in one head; the procedure is not written down until someone else uses it to sample a new cell and the author accepts it unchanged.  | scheme=example; §Succession
            SU1
            SU2
            SU3

    REC12  | Avoid anything that still needs a human per change, and anything that samples the wrong inputs.  | §What to avoid; restates §Overview ("What we don't do", OV13)
        F15  | The anti-list: better generators, better prompts, specs without tests, tests without refuters, shared-code checkers, trusting a model as a compiler, counting lines; and random / volume-weighted / product-filed sampling.  | scheme=practical; §What to avoid
            OV13
            AV1
            AV2

    # ── base: descriptive and open material no single recommendation rests on (? edge to root) ──
    base ?  | The plan's descriptive base — its vocabulary, goal and premises, the questions it does not settle, the provenance of its rules, and the design cautions carried across all cases.  | root child on ? edge (spec/ARGUMENT.md § base)
        B-GOAL  | Goal and premises: survive the transition from a legacy of unspecified working code; the three pieces look alike but differ, and the input's producer decides which case each partition is.  | §Goal, §Overview
            OV1
            OV2
            OV5
            OV6
            OV7
            OV9
            OV10
            GO1
            GO2
            GO3
            PR3
        B-GL  | Glossary — the shared vocabulary the rest of the plan is stated in.  | §Words used in this document
            GL1
            GL2
            GL5
            GL6
            GL7
            GL8
            GL9
            GL10
        B-OPEN  | What the plan does not settle — extraction, the compression bet for new behaviour, the before-method PDF bug count, counting producers, keeping the security blind, training, timing review.  | §What this does not settle; §Appendix grade (AP0)
            NS1
            NS2
            NS3
            NS4
            NS5
            AP0
        B-PROV  | Where the rules came from — internal projects the reader may not know, several self-graded [cite path] (unverified in the document).  | §Appendix
            AP1
            AP2
            AP3
        B-DESIGN  | Design cautions carried across the cases: a refuter checks behaviour not design, frozen is not forever, and where a statement lives in the repo.  | §Tests are code too, §What a statement is
            ST10
            TC6
            TC7
