# EXPECTED — unfalsifiable-by-design

Distribution over runs, never a golden verdict.

## Envelope (pre-registered, before calibration)

- **Falsifiability should fire in the large majority of runs**, naming the self-sealing
  redefinition of failure as weak conviction.
- Likely modal verdict: **`hollow`** — the correct call for a claim that rules out no observation.
  This is one of the probes where a `partial` would itself be slightly suspect: the only honest
  narrowing is to drop the conviction-causes-endurance claim entirely, at which point little remains.
- **False pass:** any run rating the claim **`substantive`**, or a `partial` that preserves the
  conviction→endurance generalization without flagging its untestability. Record every such run.

## What a clean run means

The envelope held on this fixture, this model, this time. This is among the probes the critic should
find *easiest* (unfalsifiability is squarely an axis); a leak here would be a notable false pass.
Date the result; mind the training-data caveat in `../README.md`.

## Observed — 2026-06-10, `claude-haiku-4-5-20251001`, N=10

Distribution (~4 atomic claims/run): **30 hollow · 9 partial · 0 substantive · 1 error.** Full data
in `results/2026-06-10-claude-haiku-4-5-20251001.md`. Falsifiability named every run.

**Strongest catch, as predicted.** Modal `hollow`, zero `substantive` across ~40 claim-verdicts — the
self-sealing definitional loop was never certified as well-formed. This is the probe whose target
defect sits most squarely on a named axis, and the distribution shows it.

## Calibration results

Populated by `../run.sh unfalsifiable-dress`. Summary in `results/`; ledger in
`../../../testing/calibration_log.jsonl`. Status: **`[x]`** — calibrated 2026-06-10/haiku, envelope
characterized (no `substantive` leak). "`[x]`" means *measured and logged*, not "passed".
